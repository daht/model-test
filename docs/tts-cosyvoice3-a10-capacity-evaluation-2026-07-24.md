# CosyVoice 3 在 NVIDIA A10 上的 WebSocket 流式容量与瓶颈评估

日期：2026-07-24

对象：单张 NVIDIA A10 24 GB、`Fun-CosyVoice3-0.5B-2512`、真实 WebSocket PCM 流式输出

## 1. 执行摘要

本报告包含两个阶段的评估对比：**旧版 PyTorch 单进程锁架构 (2026-07-24)** 与 **官方 Triton + TensorRT-LLM 部署架构 (2026-07-26)**。通过独立客户端访问 `WS → FastAPI → TTS Backend → binary PCM` 完整链路，并与 A10 服务端 GPU、容器及应用日志时间线对齐。

### 核心结论对比

| 部署架构 | 单并发 TTFA P95 | 最高观察吞吐 (RTFx) | 并发 2+ 排队状况 | 连续播放 (Chunk Gap P95) | 结论概括 |
| --- |:---:|:---:|:---:|:---:| --- |
| **旧 PyTorch 单进程锁** | 3.522 秒 | 1.33–1.38 | 全局锁严重串行化 | 3.55 秒 | 单进程锁瓶颈，首包与吞吐均未达标 |
| **新 Triton+TRT-LLM** | **0.593 秒** | **2.257** | 并发 2 起出现延迟排队 | 1.48 秒 | **首包达标**；算力瓶颈突破；连续播放 SLO 仍未达标 |

- **首包延迟瓶颈彻底打破**：官方 Triton + TensorRT-LLM 架构下，单并发低负载 TTFA P95 可达 **0.593 秒**（P50 达 `0.511 秒`），成功突破 `< 0.8 秒` 实时交互 SLO 门槛。
- **容量与排队边界**：在当前语料与 SLO 门槛下，低负载单并发是唯一接近达标的区间；并发 2 起即出现排队延迟（TTFA P95 升至 `1.809 秒`），当前实时交互容量仅限低并发区间。
- **吞吐平台提升**：闭环并发 8 时观察到的生成吞吐平台约为 **2.257 RTFx**（相比旧架构上限 `1.35` 提升 67%）。这代表本配置下观察到的高负载表现，但不能简易称为 A10 的绝对硬件上限。
- **待确认的 Chunk Gap 瓶颈**：所有测试档位请求均 100% 成功且无崩溃报错，但 Chunk Gap 最佳仍约 1.48 秒，未能满足连续播放 SLO（≤0.5s）。初步怀疑为 `token2wav/Flow` 与 `vocoder` 的小块生成固定开销导致，需进一步服务端分阶段 profiling 确认瓶颈。


## 2. 测试对象与判定口径

| 项目 | 配置 |
| --- | --- |
| GPU | NVIDIA A10，23,028 MiB |
| 模型 | `Fun-CosyVoice3-0.5B-2512` |
| 音色 | CosyVoice 官方 `asset/zero_shot_prompt.wav` 注册的默认零样本音色 |
| 服务链路 | 独立客户端 → 公网 WS → FastAPI → CosyVoice 3 |
| 应用协议 | MiniMax 风格事件；`/v1/tts/stream` |
| 音频 | binary `TTS1` 帧，24 kHz、mono、pcm_s16le |
| 语料 | 12 条中文诊断语料，短/中/长 1:1:1 交错消费 |
| 闭环负载 | 并发 1、2、4；每档目标 60 秒 |
| 开环负载 | 泊松到达率 0.03、0.05、0.07 RPS；每档目标 120 秒 |
| 服务版本 | `a105c34574f7b7b7656a49694ef55690704413fb` |
| 监控批次 | `20260724T042838Z-b7406e` |

本轮使用测试方案中的临时交互门槛：

- TTFA P95 ≤ 0.8 秒；
- chunk gap P99 ≤ 0.5 秒；
- 错误率 ≤ 0.1%；
- 模拟播放断流总时长为 0。

容量必须同时满足全部门槛。请求成功或音频 RTFx 大于 1，不等于达到交互式容量要求。

## 3. 客户端结果

### 3.1 闭环并发

| 并发 | 成功/请求 | 完成 RPS | 音频 RTFx | 平均在途 | TTFA P50/P95 | E2E P50/P95 | gap max | SLO |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 1 | 6/6 | 0.077 | 1.331 | 1.000 | 3.117/3.522s | 6.958/30.944s | 3.554s | 失败 |
| 2 | 7/7 | 0.092 | 1.375 | 1.970 | 10.455/30.643s | 27.361/35.810s | 3.224s | 失败 |
| 4 | 9/9 | 0.082 | 1.374 | 3.075 | 37.332/44.032s | 41.528/67.249s | 3.640s | 失败 |

并发 1→4 时音频吞吐仅从 1.331 变化到 1.374 RTFx，属于同一平台；TTFA 却增加一个数量级。请求完成 RPS 受短、中、长文本比例和档位停止后的 drain 时间影响，不能单独代表模型算力。音频 RTFx 更能反映这一轮混合语料的总体生成吞吐。

### 3.2 开环到达率

| 目标到达率 | 成功/请求 | 完成 RPS | 音频 RTFx | 平均在途 | TTFA P50/P95 | E2E P50/P95 | gap max | SLO |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 0.03 | 3/3 | 0.024 | 0.419 | 0.306 | 3.112/3.282s | 8.213/27.082s | 3.155s | 失败 |
| 0.05 | 6/6 | 0.064 | 1.091 | 0.876 | 3.213/8.431s | 7.621/33.071s | 3.462s | 失败 |
| 0.07 | 8/8 | 0.065 | 0.945 | 1.737 | 20.210/44.100s | 25.947/44.220s | 3.269s | 失败 |

`0.03 RPS` 档的平均在途仅 0.306，TTFA 仍约 3 秒，证明基础首包延迟并非排队造成。`0.05 RPS` 已出现 P95 8.431 秒的突发等待；`0.07 RPS` 的完成速率停在约 0.065 RPS，同时平均在途和 TTFA 显著增加，表现为到达速度开始超过当前串行服务速度。

开环每档样本数只有 3–8 个，因此这一轮足以定位瓶颈并否定当前 SLO，但不足以精确估计 P95/P99 或发布正式生产容量。

## 4. 服务端资源证据

监控从 04:28:38 持续到 05:01:12 UTC。为避免测试结束后的 21 分钟空闲时间稀释均值，本节资源统计使用首个模型音频事件至最后一个模型音频事件，即 04:29:05.697–04:39:49.874 UTC 的 644.177 秒有效窗口。

| 指标 | 有效窗口结果 |
| --- | ---: |
| GPU 样本数 | 1,222 |
| GPU 利用率平均/P50/P95/最大 | 44.2% / 41% / 100% / 100% |
| GPU 利用率为 0 的样本 | 27.3% |
| GPU 利用率 ≥ 90% 的样本 | 23.0% |
| 显存平均/峰值 | 4,698 / 5,253 MiB |
| 峰值显存占物理显存 | 22.8% |
| 功耗平均/P95/最大 | 105.5 / 146.5 / 157.3 W |
| 容器 CPU 平均/P95/最大 | 86.2% / 167.8% / 193.6% |
| 容器内存占主机比例平均/最大 | 6.50% / 6.56% |

Docker CPU 百分比按单核 100% 计，P95 约 1.68 个 CPU 核；结合 GPU 空闲样本和应用串行锁，没有 CPU 饱和证据。

GPU 进程采集的 3,728 个样本中始终只有一个 `python` 进程（PID 1643614），其显存范围为 4,488–5,244 MiB。测试期间没有 ASR、MT 或其他 GPU 任务竞争资源。

容器状态和日志同时满足：

- 容器持续运行，restart count 为 0，`OOMKilled=false`；
- 42 个 WS 会话 accepted/open/closed 数量一致，其中 39 个为正式请求、3 个为预热；
- 61 个文本段共生成 181 个音频块、649.28 秒音频；
- 无 `ERROR`、Traceback、CUDA OOM 或采集器错误。

全监控窗口的 GPU 平均利用率 14.6% 只反映包含长空闲尾段的整体均值，不应用于判断压测期间的 GPU 负载。

## 5. 瓶颈判定

### 5.1 主瓶颈：全局锁串行化

当前 `CosyVoiceTTSSynthesizer.stream_pcm()` 在进入模型前获取实例级 `Lock`，并在生成器全部结束后才释放。锁覆盖模型加载检查、`inference_zero_shot(..., stream=True)` 和所有音频块 yield，因此同一进程内只允许一个 utterance 执行模型推理。

```text
多个 WebSocket 已 accepted
          ↓
每个请求进入工作线程
          ↓
实例级 Lock 只放行一个 utterance
          ↓
其余请求等待上一段全部生成完成
          ↓
音频吞吐不随并发增长，TTFA 随队列增长
```

该判断由三类独立证据共同支持：

1. 代码明确把完整流式生成过程包在同一个锁内；
2. 闭环并发 1、2、4 的音频 RTFx稳定在 1.33–1.38，未出现并行吞吐收益；
3. 服务日志在多个 WebSocket 同时 accepted 后仍逐个记录 synthesis/yield 序列，没有并发模型执行；同时显存、容器 CPU 和 GPU 平均利用率均有明显余量。

因此删除锁本身并不自动等于安全并发。需要先确认 CosyVoice 模型对象、CUDA stream 和零样本音色缓存能否并发调用，再选择受控并发、微批调度或支持连续批处理的推理后端。

### 5.2 次瓶颈：流式块粒度约 4 秒

服务日志的模型块统计：

| 指标 | 结果 |
| --- | ---: |
| 音频块数 | 181 |
| 单块音频时长平均/P50/P95/最大 | 3.587 / 4.00 / 4.36 / 4.36 秒 |
| 单块模型 RTF 平均/P50/P95 | 0.788 / 0.710 / 1.097 |

模型 RTF 小于 1 表示生成速度快于播放速度，但一个典型 4 秒音频块仍需约 `4 × 0.71 ≈ 2.84` 秒才能产生。这与无排队请求约 3 秒的 TTFA、客户端约 3.2–3.6 秒的最大块间隔一致。

少数不足 1 秒的尾块会把单块 RTF 拉高，不能据此认定整段生成慢于实时。真正影响交互体验的是首块和后续块的绝对等待时间过长。

### 5.3 已排除或证据不足的方向

- **显存瓶颈：排除。** 峰值只使用 22.8% 物理显存，无 OOM。
- **其他 GPU 服务干扰：排除。** 全程仅一个 TTS Python GPU 进程。
- **容器崩溃或泄漏导致退化：排除本轮故障。** 无重启、OOM 或错误；本轮时长不足以评价长期内存泄漏。
- **网络是主瓶颈：不支持。** 网络会进入客户端 TTFA/E2E，但服务端日志显示约 3 秒的模型首块生成周期，且并发增长后的几十秒等待与串行队列一致。由于未单独保存 RTT，本轮不能量化网络的毫秒级贡献。
- **GPU 算力已经饱和：不支持。** 有效窗口平均利用率 44.2%，27.3% 样本为 0；局部 P95 达 100% 说明单次模型 kernel 会吃满 GPU，但调度存在明显空洞。

## 6. 当前容量边界

需要区分三个口径：

| 口径 | 本轮结论 |
| --- | --- |
| 满足临时交互 SLO 的容量 | **0 RPS**；最低档已因 TTFA 和 gap 失败 |
| 当前混合语料无明显排队区 | 约 `≤ 0.03 RPS`，仅为小样本方向性结果 |
| 串行实现的饱和音频吞吐 | 约 1.33–1.38 音频秒/墙钟秒 |

不能把闭环最高完成 RPS 0.092 当成生产容量：其 TTFA P95 已达 30.643 秒。也不能把零播放断流解释为低延迟；客户端是在等待约 3 秒后一次得到约 4 秒音频，随后生成速度略快于播放速度。

当前结果只适用于本轮短/中/长 1:1:1 语料。业务请求 RPS 与文本长度强相关，后续容量规划应同时使用音频秒/秒、输入字符/秒和真实业务长度分布。

## 7. 优化与复测顺序

1. **先增加服务端阶段指标。** 在 `task_finished.extra_info` 中真实填充 queue、model-first-chunk、synthesis 和 encode 时间，并暴露当前/最大队列深度，避免仅通过客户端 TTFA反推排队。
2. **验证受控并发执行。** 用并发 2 的最小实验确认同一模型实例是否线程安全、显存增量、结果一致性和吞吐收益；若不安全，不直接删除锁，改用明确的单卡调度器或支持批处理的后端。
3. **把模型输出块降到可交互范围。** 目标是 40–80 ms PCM 传输块，但传输层切块不能伪造模型首包；必须让模型或推理后端更早产出有效音频，并重新验证 `first_chunk_generated < synthesis_finished`。
4. **采用 CosyVoice 官方 Triton+TensorRT-LLM 部署路径（方案 B）。** 参照官方规范使用镜像 `soar97/triton-cosyvoice:25.06` 及 `run_cosyvoice3.sh` 完成 TRT-LLM 编译与 Triton 8001 端口服务拉起，彻底解除单进程锁限制。
5. **优化后重跑同一阶梯。** 先闭环 1/2/4/8 定位并行收益，再用开环到达率在最后通过点和首次失败点之间按 5%–10% 补档。
6. **候选容量点做 30–60 分钟 soak。** 使用每档至少 30 条且符合真实长度分布的语料，补充 WSS/TLS、音频质量、真实生产音色和 24 小时稳定性验证。

只有在最低负载先满足基础 TTFA/gap 后，提高并发才有容量意义。当前阶段不建议通过放宽 SLO 或增加等待队列来制造表面上的高并发数。

## 8. 适用边界

- 本报告是瓶颈定位和首轮容量扫描，不是生产容量验收。
- 开环档只有 3–8 个样本，P95/P99 统计不稳定；未执行边界补档和长时间 soak。
- 使用 WS，不包含 WSS/TLS、反向代理或生产负载均衡开销。
- 使用 CosyVoice 官方零样本参考音频，不代表最终生产音色的质量与性能。
- 未执行 MOS、ABX、发音准确率、音色相似度或长文本质量评估。
- 未测多实例、多进程、多卡、动态批处理和故障恢复。
- 客户端语料已按短/中/长交错，避免按长度分组造成阶段偏差；结果仍不替代真实业务长度分布。

## 9. 证据与校验

原始服务端证据保存在工作区 `log/`，不提交到 Git。归档内部 `manifest.sha256` 的 8 个文件均校验通过，`collector-errors.log` 为空。

| 证据 | SHA-256 |
| --- | --- |
| `log/20260724T042838Z-b7406e.tar.gz` | `c7e5a68d12a9cb2739686f0c9663cc36d3e0ee16d38e8ffaf98e77f2d0ddd69f` |
| `log/20260726T131918Z-ae5216.tar.gz` | `(Triton 部署全量测试监控归档)` |
| 闭环 `tts-stream-benchmark.json` | `04f4400d297146012da98b9aaf1eef810f2c73fde176d0a2215320d2bcd15a30` |
| 闭环 `tts-stream-observations.jsonl` | `1378d578840457b278d2e763a2cdd896d42dfbf81306a25d28eef091425343d6` |
| 开环 `tts-stream-benchmark.json` | `0868ee7c4a623108dd6091c36888f0cf8203ed98d74344c6c34b4483f956eaec` |
| 开环 `tts-stream-observations.jsonl` | `3d83646cfe7ecd2a03103313a631032af3dbf10ea81ed11568d8f6e261c5ddb0` |

客户端结果已保存到 `log/tts-triton-closed/`、`log/tts-triton-closed-high/` 与 `log/tts-triton-open/`。API key 未写入上述报告或服务端监控归档。

## 10. 官方 Triton + TensorRT-LLM 部署复测结果（2026-07-26）

基于 NVIDIA 官方 `soar97/triton-cosyvoice:25.06` 容器架构（Triton Inference Server + TensorRT-LLM LLM Engine）全量复测完成。

### 10.1 闭环容量测试数据 (Closed-Loop Concurrency)

| Concurrency | 成功/请求 | RPS | 音频 RTFx | 在途均值 | TTFA p50/p95 (s) | E2E p50/p95 (s) | Chunk Gap p95 (s) | SLO 状态 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | 12/12 (100%) | 0.100 | 1.456 | 1.260 | **0.511 / 0.593** | 4.813 / 31.604 | 1.480 | 首包达标 |
| 2 | 12/12 (100%) | 0.100 | 1.532 | 2.414 | 0.865 / 1.809 | 17.773 / 58.864 | 2.814 | 失败 |
| 4 | 15/15 (100%) | 0.125 | 1.851 | 5.338 | 1.535 / 3.452 | 34.723 / 107.732 | 4.451 | 失败 |
| 6 | 15/15 (100%) | 0.125 | 1.878 | 7.432 | 2.201 / 4.976 | 44.959 / 134.789 | 5.154 | 失败 |
| 8 | 19/19 (100%) | 0.158 | **2.257** | 11.752 | 2.666 / 5.214 | 45.705 / 174.241 | 6.326 | 失败 |

### 10.2 开环到达率测试数据 (Open-Loop Arrival Rates)

| Arrival Rate (RPS) | 成功/请求 | 实际 RPS | 音频 RTFx | 在途均值 | TTFA p50/p95 (s) | E2E p50/p95 (s) | Chunk Gap p95 (s) | SLO 状态 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 0.03 | 8/8 (100%) | 0.039 | 0.507 | 0.430 | **0.513 / 0.863** | 5.205 / 32.244 | 3.159 | 失败 |
| 0.05 | 17/17 (100%) | 0.066 | 0.936 | 0.998 | 0.580 / 2.592 | 7.174 / 41.027 | 2.534 | 失败 |
| 0.08 | 22/22 (100%) | 0.076 | 1.104 | 1.972 | 1.470 / 3.034 | 19.209 / 55.676 | 3.274 | 失败 |
| 0.10 | 34/34 (100%) | 0.131 | 1.936 | 12.129 | 3.752 / 7.737 | 59.751 / 243.919 | 7.738 | 失败 |

### 10.3 关键结论与优化方向

1. **首包延迟与实时容量**：官方 Triton + TensorRT-LLM 部署后，单并发低负载下 TTFA P95 可达到约 **0.593 秒**（P50 达 `0.511 秒`），吞吐较旧架构明显提升；但并发 2 起即出现延迟排队（TTFA P95 升至 `1.809s`），当前实时交互容量仅限低并发区间。
2. **吞吐上限说明**：高负载下观察到的最高生成吞吐平台约为 **2.257 RTFx**（并发 8）。当前已接近本配置下的服务饱和区，但不能称为 A10 的绝对硬件上限（需后续在更高并发、显存、功耗及采样步数控制下进一步验证）。
3. **Chunk Gap 瓶颈待确认**：所有测试档位请求均 100% 成功且无崩溃报错，但 Chunk Gap 最佳仍约 1.48 秒，未能满足连续播放 SLO（≤0.5s）。Flow/token2wav/vocoder 是 Chunk Gap 的待确认瓶颈，初步怀疑为小块生成固定开销所致，需在服务端增加分阶段耗时 profiling（区分 LLM、Flow、Vocoder 和网络发送时间）后再决定优化方向。


